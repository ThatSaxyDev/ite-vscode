Open iTE from:

- the `iTE` status-bar item
- the Command Palette with `iTE: Open iTE`

The extension launches iTE for the workspace you already have open, then displays Reup in a dedicated VS Code terminal.

If iTE is not installed yet, the extension opens the official installer in that terminal and starts iTE after installation completes.
